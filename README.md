### TypeORM one-to-one relation

Cannot use primary key as foreign key in Profile entity

Produces the following error
```
TypeError: Cannot create property 'user' on string 'my-user-id'
```

```shell
npm run start
```